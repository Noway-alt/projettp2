<nav class="navbar navbar-expand-lg mb-4">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_quiz_index') }}">🎯 Quiz App</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_quiz_take_index') }}">
                        <i class="bi bi-play-circle"></i> Passer un Quiz
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_quiz_index') }}">
                        <i class="bi bi-grid"></i> Gérer les Quiz
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_question_index') }}">
                        <i class="bi bi-question-circle"></i> Questions
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_answer_index') }}">
                        <i class="bi bi-chat-dots"></i> Réponses
                    </a>
                </li>
                {% if app.user %}
                    <li class="nav-item">
                        <span class="nav-link text-muted">
                            <i class="bi bi-person-circle"></i> {{ app.user.email }}
                        </span>
                    </li>
                {% endif %}
            </ul>
        </div>
    </nav>